function adicionar(numeros){
    let numeroDigitado = window.document.querySelector('#numero').value;
    if(numeroDigitado.length == 0){
        alert("[ERRO] Por favor, insira um número de 1 à 100!")
    }

    let numCovertido = Number(numeroDigitado);

    numeros = [];

    numCovertido = numeros;
    for(let c; numCovertido.length < c; c++){
        console.log(c[numCovertido])
        //voltarei
    }
}